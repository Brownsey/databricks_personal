bundle:
  name: hf-model-deployment

include:
  - resources/*.yml

variables:
  catalog:
    default: workspace
  schema:
    default: ml_models

targets:
  dev:
    default: true
    mode: development
    workspace:
      profile: DEFAULT

  prod:
    workspace:
      profile: DEFAULT
