bundle:
  name: hf-model-deployment

include:
  - resources/*.yml

variables:
  catalog:
    default: workspace
  schema:
    default: ml_models

targets:
  dev:
    default: true
    mode: development
    workspace:
      host: https://dbc-dfbd14c3-c3d3.cloud.databricks.com

  prod:
    workspace:
      host: https://dbc-dfbd14c3-c3d3.cloud.databricks.com
